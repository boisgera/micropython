# MicroPython Async

Test with `pixi run main`.

Given that the conda-forge package is old, I have included a `setup` pixi task
that downloads, builds and install locally micropython for unix. This task is
triggered automatically when you run `pixi run main`.

